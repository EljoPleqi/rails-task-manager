<div class ="hero-container">
  <h1><%= @task.name %></h1>
  <p><%= @task.details %></p>
  <p><%= @task.completed ? "Dead" : "Alive"%></p>
  <%=link_to 'Edit Hero', edit_task_path(@task), class: 'btn' %>
  <%=link_to 'Delete Hero', destroy_task_path(@task), method: :delete , class: 'btn' %>
</div>
